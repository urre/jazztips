---
---

<style>
  @media (min-width: 1200px) {
    .record-layout {
      display: grid;
      grid-template-columns: auto minmax(0, 1fr);
      column-gap: calc(var(--fluid-gap) * 10);
    }
  }
</style>

<div class="record-layout">
  <slot />
</div>
