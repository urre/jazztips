---
export interface MusicianListProps {
  musicians: Musician[];
}

export interface Musician {
  name: string;
  instrument: string;
}

const { musicians } = Astro.props;
---

<fieldset>
  <legend>Musicians</legend>
  <ul>
    {
      musicians.map((musician: Musician) => (
        <li class="musician">
          <div class="name">
            {musician.name}

            <a
              href={`https://www.discogs.com/search/?q=${musician.name}&type=artist`}
              title={`Explore ${musician.name} on Discogs`}
              target="_blank"
            >
              <em>Search Discogs →</em>
            </a>
          </div>
          <div class="instrument">{musician.instrument}</div>
        </li>
      ))
    }
  </ul>
</fieldset>

<style>
  .ul {
    display: flex;
    flex-wrap: wrap;
    gap: 4rem;
  }

  .name {
    flex-basis: 30%;
    display: inline-flex;
    gap: 2rem;
    align-items: center;
    font-size: 1.5rem;
  }

  .name a {
    opacity: 0;
    font-size: smaller;
    text-decoration: underline;
    text-underline-offset: 4px;
    font-size: 0.85rem;
  }

  .name:hover a {
    opacity: 1;
  }

  .instrument {
    flex-basis: 20%;
  }

  .link a {
    display: flex;
    align-items: center;
    gap: 1rem;
  }
</style>
