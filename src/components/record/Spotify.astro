---
import Embed from "./embeds/Embed.astro";

export interface Props {
  spotify: string;
}

const { spotify } = Astro.props;

const embedCode = spotify.replace(".com/", ".com/embed/");
---

<Embed embedCode={embedCode} />
