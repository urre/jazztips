---
export interface Props {
  spotify: string;
}

const { spotify } = Astro.props;

const embedCode = spotify.replace(".com/", ".com/embed/");
---

<iframe
  class="rounded"
  src={embedCode}
  allowtransparency="true"
  frame-border="0"
  allow="encrypted-media"></iframe>
