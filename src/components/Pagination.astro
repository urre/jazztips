---
import Button from "./Button.astro";

export interface Props {
  prev?: string;
  next?: string;
}

// Use Astro's direct access to props
const { prev, next } = Astro.props;
---

<style>
  nav {
    display: flex; /* Flexbox is more versatile for responsive layouts */
    justify-content: flex-end;
    align-items: center;
    gap: 1rem;
    padding: 2rem 0;
    list-style: none;
  }

  /* Responsive improvements for smaller screens */
</style>

<nav aria-label="Sidnavigering">
  {
    prev && (
      <Button href={prev} aria-label="Gå till föregående sida">
        Föregående
      </Button>
    )
  }
  {
    next && (
      <Button href={next} aria-label="Gå till nästa sida">
        Nästa
      </Button>
    )
  }
</nav>
