---
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";

import { getCollection } from "astro:content";

// Get all posts from the collection
const records = await getCollection("records"); // or whatever your collection name is

// Extract tags from post data instead of frontmatter
const tags = [...new Set(records.map((record) => record.data.tags).flat())];
const uniqueTagCount = tags.length;

const pageTitle = "Tag Index";
---

<html lang="en">
  <head>
    <BaseHead title={pageTitle} description="Tags" />
  </head>

  <body>
    <Header />
    <main>
      <h1>View content by Tags</h1>
      <p>Currently <em>{uniqueTagCount}</em> tags</p>
      <ul class="tags">
        {
          tags
            .sort((tag1: string, tag2: string) => tag1.localeCompare(tag2))
            .map((tag) => (
              <li>
                <a rel="tag" class="button" href={`/tags/${tag}`}>
                  {tag}
                </a>
              </li>
            ))
        }
      </ul>
    </main>
  </body>
</html>
